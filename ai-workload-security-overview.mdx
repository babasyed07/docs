---
title: "AI Workload Security Overview"
description: "Intent-based, runtime authorization for every action your AI agents take."
---

**AI Workload Security** applies Reva's [Intent & Behavior-Based Access Control](/) to your agentic systems. As an agent runs, every action it takes — invoking another agent, a tool, an MCP server, or a model — is authorized in real time against your policies **before it happens**, then recorded for audit. Authorization follows the agent's *intent and behavior*, not just its identity.

## Why Agents Need More Than Identity

An agent's identity tells you *who* it is — not whether what it's about to do is appropriate. Because agents act autonomously, at machine speed, and on behalf of others, identity-only controls leave three gaps that compound across an agent chain:

- **Excessive agency** — an over-permissioned agent takes destructive actions far beyond what anyone intended.
- **Intent decay** — context shifts at every hop, so a single up-front permission check stops reflecting what's really happening.
- **Confused deputy** — agents delegate access to other agents and systems, creating unbounded chains with no clear accountability.

Reva closes these gaps by authorizing **every hop** against the agent's intent and runtime context.

## What Reva Authorizes

Reva evaluates each hop in an agent chain as a distinct authorization decision:

| Call | PDP action | Example |
|:-----|:-----------|:--------|
| **User → Agent** | `invokeAgent` | A user asks the supervisor agent to handle a request |
| **Agent → Agent** | `invokeAgent` | The supervisor delegates to a billing sub-agent |
| **Agent → Tool / MCP server** | `invokeTool` | An agent calls a tool exposed by an MCP server |
| **Agent → Model** | `invokeModel` | An agent calls an LLM to reason over a prompt |
| **Agent → API** | `read` / `write` / `delete` | An agent calls a REST API |

<Note>
  MCP **discovery** calls (`tools/list`, `prompts/list`, `resources/list`, `initialize`) are not policy-checked. Only **invocations** (`tools/call`, `prompts/get`, `resources/read`) are evaluated.
</Note>

## How It Works

Three pieces work together to authorize an agent chain:

- **Reva PDP (Policy Decision Point)** — evaluates Cedar policies and returns allow/deny.
- **Reva Trust Gateway (RTG)** — issues short-lived, task-scoped transaction tokens that travel with each hop.
- **`reva-ai` SDK** — a thin layer you add to your agents: an **inbound guard** that checks each incoming call, and an **outbound proxy** that authorizes and token-forwards each outgoing call.

```
                ┌──────────────────────────────┐
                │   Reva PDP (Decision Point)   │
                │   + Reva Trust Gateway        │
                │   allow / deny · issue token  │
                └──────────────────────────────┘
                       ▲                ▲
        evaluate       │                │   issue a
        inbound call   │                │   transaction token
                       │                │
  User ──Bearer──▶  Agent A  ─────────▶  Agent B  ─────────▶  Tool / MCP / Model
                  @reva_ai_authorise      revaclient.proxy
                  (inbound guard)         (outbound, token-forwarded)
```

1. A user (or an upstream agent) calls an agent, carrying a bearer token.
2. The agent's **inbound guard** builds an evaluation request — subject, action, resource, principal, and context (such as `onBehalfOf`, environment, and the prompt/intent) — and calls the PDP.
3. The PDP evaluates the active Cedar policies in the policy store and returns **allow** or **deny**.
4. On **allow**, the agent's handler runs. On **deny**, the call is rejected — Reva is **fail-closed** by default.
5. For each **outbound** call, the SDK proxy asks the RTG to issue a short-lived **transaction token** and forwards it to the next hop, which re-evaluates on its own inbound boundary.
6. Every decision — allow or deny — is written to the [decision logs](/ai-workload-decision-logs), correlated by a `threadId` so you can trace an entire agent chain end to end.

No code? You can call the [Agent Authorization Evaluation API](/ai-workload-developer-evaluation-api) directly from your own enforcement point instead.

## Precision Enforcement and Audit

Because enforcement is policy-driven, Reva stays in control even when the agent isn't:

- **Deterministic** — an inappropriate action is blocked even if the agent's reasoning or LLM is compromised by prompt injection.
- **Zero Standing Privileges** — each hop is authorized just-in-time for the task's intent, and the scoped transaction token expires immediately, so privileges never linger.
- **Chain-of-custody** — every hop shares a `threadId`, giving you an end-to-end, replayable record of who did what, on whose behalf, and why.
- **Guarded at runtime** — pair policies with [runtime guardrails](/guardrails/overview) to screen content, context, behavior, and intent on every request, and route risky actions to human approval.

## Capabilities at a Glance

Reva's AI Workload Security capabilities span runtime authorization, identity delegation, tool governance, and audit.

<Card title="Capability & Maturity Matrix" icon="table-list" href="/ai-workload-capability-matrix">
  See exactly what's **Available** today, what's in **Beta**, and what's on the **Roadmap**.
</Card>

## Next Steps

<CardGroup cols={3}>
  <Card title="Model Your Agents" icon="diagram-project" href="/ai-workload-context-modeling">
    Define your agents, tools, models, and MCP servers as Reva entities.
  </Card>
  <Card title="Design Policies" icon="shield-halved" href="/ai-workload-policy-design">
    Write context-based, on-behalf-of, and intent-based agent policies.
  </Card>
  <Card title="Integrate the SDK" icon="code" href="/ai-workload-developer-sdk">
    Add `reva-ai` to your agents, or call the PDP API directly.
  </Card>
</CardGroup>
