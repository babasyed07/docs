---
title: "Agent Authorization Evaluation API"
description: "Call the Reva PDP directly to authorize agent-chain calls without the SDK."
---

If you can't (or don't want to) embed the [`reva-ai` SDK](/ai-workload-developer-sdk), call the Reva PDP directly from your own enforcement point. You send the details of a call — who is calling, what they're doing, on what resource, with what context — and the PDP returns an allow/deny decision.

| Approach | Use when |
|:---------|:---------|
| **SDK** ([guide](/ai-workload-developer-sdk)) | You control the agent code and want drop-in inbound/outbound authorization. |
| **Evaluation API** (this page) | You enforce at a gateway/proxy, use an unsupported language, or want full control over the request. |

## Endpoint

| Method & path | Purpose |
|:--------------|:--------|
| `POST /pdp/access/v1/ai/evaluation` | Evaluate a single agent-chain call (user → agent, agent → agent, agent → tool / MCP / model) and return **allow / deny**. |

The base URL is your tenant's API host — for example `https://api.pr06.preview.reva.ai`.

## Authentication & Headers

| Header | Required | Description |
|:-------|:---------|:------------|
| `Content-Type` | yes | `application/json` |
| `Authorization` | yes | `Bearer <PDP Auth Token>` — your PDP Auth Token (see below) |
| `policyStoreId` | yes | The policy store to evaluate against |
| `x-ms-correlation-id` | recommended | A correlation id used to trace the request across hops |

Get your **PDP Auth Token** from the **Developer Settings** page, under the **PDP Auth Token** section. It is a long-lived platform credential — not a transaction token.

## Request Body

The request is a JSON envelope describing one authorization decision. The parts below are the common building blocks — **the values are representative, not a fixed schema.** What you actually send (especially inside `context`) is whatever your policies and guardrails reference. The [example payloads](#example-bodies-by-hop) are your best reference.

| Part | What it carries |
|:-----|:----------------|
| `subject` | Who is making the call — `{ type, id, name }` (`User` or `Agent`), with optional `properties`. |
| `action` | The operation, e.g. `{ "name": "invokeAgent" }` — `invokeAgent`, `invokeTool`, `invokeModel`, and so on. |
| `resource` | What is being called — `{ type, id, name, properties }`. `type` is typically `Agent`, `Tool`, or `MCPServer`. |
| `principal` | The originating end user the chain runs for. |
| `context` | The signals your policies evaluate. Commonly `onBehalfOf` (the delegating user and attributes such as `riskTier`) and `environment` (`requestId`, `time`, `sourceIp`, `geo`). It may also carry guardrail signals (for example `blockedTermInUserMessage`, `blockedTermInInput`, `isBlockedTool`) or any custom attribute your policies reference. |
| `transmission` | The prompt/intent of the call — `promptKey`, `role`, `contentType`, `content`. |
| `inputValues` | The structured arguments passed to the tool/agent, e.g. `{ "policy_id": "P-10234" }`. |
| `session` | Conversation correlation — `id` (thread), `turn`, `startedAt`. |
| `history` | Prior hop(s), recursively, for chain-of-custody. Omit at the first hop. |

<Note>
  These fields are **representative, not an exhaustive contract.** Send the attributes your policies actually use — Reva evaluates the `subject`, `action`, `resource`, and whatever you place in `context`. Use the example payloads below as your template.
</Note>

## Example Request

```bash cURL
curl -X POST "https://api.pr06.preview.reva.ai/pdp/access/v1/ai/evaluation" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <pdp-auth-token>" \
  -H "policyStoreId: 3b373af8-3fb4-4c31-b69a-f72e847eae08" \
  -H "x-ms-correlation-id: aaaa0000-1111-2222-3333-444455556666" \
  --data '{
    "subject": { "type": "Agent", "id": "underwriter-copilot", "name": "underwriter-copilot" },
    "action": { "name": "invokeTool" },
    "resource": { "type": "Tool", "id": "fetch-risk-score", "name": "Fetch Risk Score" },
    "principal": { "type": "User", "id": "amit" },
    "context": {
      "onBehalfOf": { "type": "User", "id": "amit", "properties": { "riskTier": "MEDIUM" } },
      "environment": { "requestId": "req-a2t-allow", "time": "2026-06-21T10:00:00Z", "sourceIp": "10.0.4.12", "geo": "IN" },
      "blockedTermInUserMessage": false,
      "blockedTermInInput": false,
      "isBlockedTool": false
    },
    "transmission": { "promptKey": "userQuery", "role": "user", "contentType": "text/plain", "content": "Fetch the risk score for policy P-10234" },
    "inputValues": { "policy_id": "P-10234" },
    "session": { "id": "thread-uc-tool-allow", "turn": 1, "startedAt": "2026-06-21T10:00:00Z" }
  }'
```

<Warning>
  The `Authorization` bearer is your **PDP Auth Token** (from Developer Settings) — a secret. Never commit or log a real token; the value above is a placeholder.
</Warning>

## Example Bodies by Hop

The same endpoint evaluates every hop type. Later hops carry a recursive `history` block for chain-of-custody.

<CodeGroup>
```json User → Agent
{
  "subject":   { "type": "User", "id": "john" },
  "action":    { "name": "invokeAgent" },
  "resource": {
    "type": "Agent",
    "id": "shipment-supervisor-agent",
    "name": "shipment-supervisor-agent",
    "properties": {
      "endpoint": { "url": "/agents/shipment-supervisor", "type": "AGENT" }
    }
  },
  "principal": { "type": "User", "id": "john" },
  "context": {
    "onBehalfOf": { "type": "User", "id": "john", "properties": { "riskTier": "LOW" } },
    "environment": { "requestId": "req-u2a-001", "time": "2026-06-18T10:00:00Z", "sourceIp": "10.0.4.12", "geo": "IN" }
  },
  "transmission": {
    "promptKey": "userQuery",
    "role": "user",
    "contentType": "text/plain",
    "content": "Track shipment SH-2024-0042 and check compliance status"
  },
  "session": { "id": "thread-sc-001", "turn": 1, "startedAt": "2026-06-18T10:00:00Z" }
}
```

```json Agent → Agent
{
  "subject": {
    "type": "Agent",
    "id": "shipment-supervisor-agent",
    "properties": { "origin": { "url": "/agents/shipment-supervisor", "type": "AGENT" } }
  },
  "action":   { "name": "invokeAgent" },
  "resource": {
    "type": "Agent",
    "id": "billing-agent",
    "name": "billing-agent",
    "properties": { "endpoint": { "url": "127.0.0.1:9093/v1/run", "type": "AGENT" } }
  },
  "principal": { "type": "User", "id": "john" },
  "context": {
    "onBehalfOf": { "type": "User", "id": "john", "properties": { "riskTier": "LOW" } },
    "environment": { "requestId": "req-a2a-001", "time": "2026-06-18T10:00:02Z", "sourceIp": "10.0.4.12", "geo": "IN" }
  },
  "transmission": {
    "promptKey": "userQuery",
    "role": "user",
    "contentType": "text/plain",
    "content": "Generate billing report for shipment SH-2024-0042"
  },
  "session": { "id": "thread-sc-001", "turn": 2, "startedAt": "2026-06-18T10:00:00Z" },
  "history": {
    "prompt": "Track shipment SH-2024-0042 and check compliance status",
    "subject": { "type": "User", "id": "john" },
    "action": { "name": "invokeAgent" },
    "resource": { "type": "Agent", "id": "shipment-supervisor-agent" },
    "issuer": "john",
    "threadId": "thread-sc-001"
  }
}
```

```json Agent → Tool
{
  "subject": {
    "type": "Agent",
    "id": "billing-agent",
    "properties": { "origin": { "url": "127.0.0.1:9093/v1/run", "type": "AGENT" } }
  },
  "action": { "name": "invokeTool" },
  "resource": {
    "type": "MCPServer",
    "id": "rate-mcp",
    "name": "reva-rate-mcp",
    "properties": { "endpoint": { "url": "127.0.0.1:9102/mcp", "type": "MCP" } },
    "tool": "reva-rate-mcp"
  },
  "principal": { "type": "User", "id": "john" },
  "context": {
    "onBehalfOf": { "type": "User", "id": "john", "properties": { "riskTier": "LOW" } },
    "environment": { "requestId": "req-a2t-001", "time": "2026-06-18T10:00:05Z", "sourceIp": "10.0.4.12", "geo": "IN" }
  },
  "transmission": {
    "promptKey": "query",
    "role": "user",
    "contentType": "text/plain",
    "content": "Calculate shipping rate for 50kg package from Mumbai to Delhi"
  },
  "session": { "id": "thread-sc-001", "turn": 3, "startedAt": "2026-06-18T10:00:00Z" },
  "history": {
    "prompt": "Generate billing report for shipment SH-2024-0042",
    "subject": { "type": "Agent", "id": "shipment-supervisor-agent" },
    "action": { "name": "invokeAgent" },
    "resource": { "type": "Agent", "id": "billing-agent" },
    "issuer": "shipment-supervisor-agent",
    "threadId": "thread-sc-001",
    "history": {
      "prompt": "Track shipment SH-2024-0042 and check compliance status",
      "subject": { "type": "User", "id": "john" },
      "action": { "name": "invokeAgent" },
      "resource": { "type": "Agent", "id": "shipment-supervisor-agent" },
      "issuer": "john",
      "threadId": "thread-sc-001"
    }
  }
}
```
</CodeGroup>

## Responses

On **allow**, evaluation returns a positive decision:

```json
{ "decision": true }
```

On **deny**, the decision is negative and a reason is included so you can diagnose the policy outcome:

```json
{
  "decision": false,
  "context": { "reason": "authorization denied by policy" }
}
```

Every decision — allow or deny — appears in the [decision logs](/decision-logs), correlated by the session `threadId`.

## See Also

<CardGroup cols={2}>
  <Card title="PDP Edge API Reference" icon="book" href="/api-reference/introduction">
    The generated reference for the published authorize and search operations.
  </Card>
  <Card title="Python SDK" icon="code" href="/ai-workload-developer-sdk">
    Skip manual requests — let the SDK build and send them.
  </Card>
</CardGroup>
